// import { create } from "zustand";

// export interface CreaPanelState {
//     isOpen: boolean;
//     toggle: () => void;
//     openPanel: () => void;
//     closePanel: () => void;
// }

// export const useCreaPanel = create<CreaPanelState>((set) => ({
//     isOpen: false,
//     toggle:() => set(state => ({ isOpen: !state.isOpen })),
//     openPanel:() => set({ isOpen: true }),
//     closePanel:() => set({ isOpen: false })
// }))
